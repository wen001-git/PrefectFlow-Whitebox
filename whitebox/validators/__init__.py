"""Per-servicer validator packages live here.

Each validator is:
    whitebox/validators/<servicer>/<name>.py    -- Python implementation
    whitebox/validators/<servicer>/<name>.yaml  -- metadata (description, business rule, I/O)
    whitebox/validators/<servicer>/<name>.sql   -- raw SQL (also consumed by sqlglot)
"""
