> **动态文档** — 遵循 AGENTS.md § 6.11 三层行为标记规范。
> `[CONFIRMED]` = 已通过源码 + 物理基准 XLSX 验证 ·
> `[VERIFY]` = 仅从源码推断，尚未通过物理产出物确认 · `[FOUND-DURING-B6]` = B6 编写期间发现。

> **关卡依赖**（AGENTS.md § 6.11 + plan.md § 4.2）：
> **P2.1 及后续所有阶段**均需等待 G1 ∧ G2a ∧ G2b ∧ G3 全部关闭且 B 系列文档（B1–B6）冻结后方可启动。
> P2.0（架构冻结）可作为纯设计工作推进。
>
> | 关卡 | 说明 | 状态 |
> |---|---|---|
> | G1 | Stage 1 走查评审 | ✅ 已关闭（暂时批准，AGENTS § 6.11） |
> | G2a | 输入快照冻结（Redshift → 本地 Parquet） | ⏸ 等待操作员操作 |
> | G2b | 物理基准 XLSX 冻结 | ⏸ 等待操作员操作 |
> | G3 | V1–V12 升级为 `[CONFIRMED]` | ⏸ 等待用户签字 |

> **目的**：提供 Stage 2 完整的分阶段开发计划（P2.0 → P2.4 → P3），
> 包含来自 7.0-module-boundaries 的模块级细节、关卡先决条件、退出标准、
> 风险登记册、待决策记录及实施 todo 种子提案。
>
> **目标读者**：所有 Stage 2 实施工程师；技术负责人；评审 B6 产出物的用户；
> 恢复 Stage 2 工作的未来会话 Agent。
>
> **版本历史**
>
> | 日期 | 作者 | 变更 |
> |---|---|---|
> | 2026-05-28 | Copilot CLI agent | v1 — 初版（B6）。重述 plan.md § 4.4 阶段划分，补充来自 7.0-module-boundaries 的模块级细节。增加退出标准、来自 plan.md § 3 的精化风险登记册、决策记录 Q1–Q4 以及 T-C todo 种子提案。 |

# 9.0 开发计划（Stage 2）

---

## 1. 阶段总览

以下阶段划分重述自 `plan.md § 4.4`（会话 `4cd52a8e-d034-4def-84a0-04057dd64872`），
并结合 `7.0-module-boundaries.en.md` 中的模块级粒度进行了细化。

```
G1 ∧ G2a ∧ G2b ∧ G3 全部关闭
∧ B 系列文档 B1–B6 全部冻结（P2.0 退出标准）
              │
              ▼
P2.0  架构冻结
      数据模型 + 引擎 API + UI 契约签字
      （仅设计——无实施代码）
              │
              ▼
P2.1  MRC 引擎实施
      M1 摄入 + M2 转换 + M3 验证器
      + M8 注册表 → 5 张工作表 → DataFrame 负载
              │
              ▼
P2.2  XLSX 渲染器实施
      M4 工作表 + M5 渲染器 → 单元格一致性验收
      （openpyxl 版本在本阶段锁定）
              │
              ▼
P2.3  UI 实施（8 个功能）
      M6 API + M7 UI（开始前需确定技术栈）
              │
              ▼
P2.4  MRC 验收
      G2 基准 + 8 功能演示 + 用户签字
              │
              ▼
P3    多服务商推广
      Arvest → CC5 → Selene → SLS → Scattered
      （使用 P2.0 预留的可扩展性插槽）
```

_图 9.0.1 — Stage 2 阶段图。每个箭头要求上方阶段达到退出标准。G 关卡为明确的硬性先决条件，而非软性依赖。_

---

## 2. 各阶段产出物与退出标准

### P2.0 — 架构冻结

**目标**：确认 B1–B6 设计产出物内部一致、交叉引用已解决、G2 关闭前可解决的 `[VERIFY]` 条目已解决，且模块边界契约（B6 `7.0-module-boundaries.en.md`）已签字确认。

**产出物**（以下 B 系列文档构成架构冻结）：

| 文档 | P2.0 退出时状态 |
|---|---|
| `docs/stage2/1.0-feature-list.{zh,en}.md` | 最终版；B1↔B5 交叉对照已理解 |
| `docs/stage2/2.0-srs.{zh,en}.md` | 最终版；37 FR + 12 NFR 稳定 |
| `docs/stage2/3.0-data-model.{zh,en}.md` | 最终版；B3 模型 Schema 稳定 |
| `docs/stage2/4.0-validator-registry.{zh,en}.md` | 最终版；注册表结构稳定 |
| `docs/stage2/5.0-extensibility-spec.{zh,en}.md` | 最终版；8 个挂钩点稳定 |
| `docs/stage2/6.0-ui-architecture.{zh,en}.md` | 最终版，Q2 技术栈选择除外 |
| `docs/stage2/7.0-module-boundaries.{zh,en}.md` | **最终版；这是 P2.0 契约** |
| `docs/stage2/8.0-servicer-onboarding.{zh,en}.md` | 最终版；入驻工作流稳定 |
| `docs/stage2/9.0-dev-plan.{zh,en}.md` | 最终版；即本文档 |

**退出标准**：

- [ ] G1 ∧ G2a ∧ G2b ∧ G3 全部关闭。
- [ ] 上述所有 B 系列文档通过 `tools/stage_doc_checks.py` 零 P0 失败。
- [ ] `pytest -q` 全部通过（Stage 2 尚无代码——仅 Stage 1 回归测试）。
- [ ] `mkdocs build --strict` 成功，`docs/stage2/` 中 9 个文档对全部包含在内。
- [ ] 待确认问题 MB-2 和 MB-3（模块边界 `7.0-module-boundaries.en.md § 7`）已解决。
- [ ] Q2 技术栈决策已确定（plan.md § 5 Q2）——解锁 P2.3 设计所需，P2.1/P2.2 不依赖此项。
- [ ] 每项解决均已在 `decisions.md` 中添加条目。

**涉及模块**：仅设计；无代码。

---

### P2.1 — MRC 引擎实施

**目标**：实施 M1（摄入）、M2（转换）、M3（验证器）、M8（注册表），端到端地从冻结 Parquet 快照产出 `list[ValidatorResult]`。

**有序实施序列**：

| 步骤 | 模块 | Todo（提案 ID） | 依赖 |
|---|---|---|---|
| 1 | M8 `registry` | `stage2-mrc-registry-impl` | P2.0 退出 |
| 2 | M1 `ingestion` | `stage2-mrc-ingestion`（现有已阻塞 todo） | M8 |
| 3 | M2 `transform` | `stage2-mrc-transform-impl` | M1 |
| 4 | M3 `validators`（5 个验证器） | `stage2-mrc-engine`（现有已阻塞 todo） | M2, M8 |
| 5 | M3 `validators` CTE 套件（H3） | `stage2-mrc-cte-harness-impl` | M3 步骤 4 |

**单元格一致性套件门控每个步骤**：每个模块完成后，对 Mock 固定数据运行 `pytest tests/test_cell_identity.py`；G2b 关闭后才进行完整真实基准运行。

**退出标准**：

- [ ] 5 个 MRC 验证器对 `tests/fixtures/` 合成数据无误执行。
- [ ] `ValidatorResult.cell_annotations` 与合成固定数据手工验证的预期高亮集合匹配。
- [ ] V2 和 V3 捕获到 `MrcIntermediateCTEs`（B3 § 2.5，H3 挂钩）；V1/V5 返回 `None`。
- [ ] `pytest tests/test_engine_mrc.py -q` 全部通过。
- [ ] `ruff check .` 零违规。
- [ ] `mypy --strict whitebox/`（仅新代码）零错误。
- [ ] M1–M3 代码中无任何针对 `ServicerId` 字符串的 `if/elif` 分支（反模式 AP1）。

**涉及模块**：M1, M2, M3, M8。

---

### P2.2 — XLSX 渲染器实施

**目标**：实施 M4（工作表）和 M5（渲染器），使 XLSX 输出在全部 5 张工作表上与 `baselines/mrc/2026-04-30/validation_report.xlsx` 达到单元格一致性。

**P2.2 开始时的关键操作**：在 `pyproject.toml` 中**锁定 openpyxl 版本**（与生成黄金基准 XLSX 所用版本相同，由 MB-5 在 P2.0 退出时解决）。参见 plan.md § 3 风险 R4。

**有序实施序列**：

| 步骤 | 模块 | Todo（提案 ID） | 依赖 |
|---|---|---|---|
| 1 | M4 `sheets`（5 个负载组装器） | `stage2-mrc-sheets-impl` | P2.1 退出 |
| 2 | M5 `renderer`（openpyxl） | `stage2-mrc-xlsx-renderer`（现有已阻塞 todo） | M4 |
| 3 | 单元格一致性套件（验收关卡） | `stage2-mrc-cell-identity-harness`（现有已阻塞 todo） | M5 |

**退出标准**：

- [ ] `openpyxl` 版本已在 `pyproject.toml` 中锁定；`uv.lock` 已更新；版本已记录在 `decisions.md` 中。
- [ ] 全部 5 张工作表的 `BaselineComparison.passed == True`（对照 `baselines/mrc/2026-04-30/validation_report.xlsx`）。
- [ ] ch 1.6 § 9 中所有 V1–V12 渲染属性从 `[VERIFY]` 升级为 `[CONFIRMED]`。
- [ ] `pytest tests/test_cell_identity.py -q` 全部通过（P0 验收关卡）。
- [ ] `pandi_diff` 高亮豁免（ch 1.5 § 3.3）通过单元格一致性检查。

**涉及模块**：M4, M5。

---

### P2.3 — UI 实施（8 个功能）

**目标**：实施 M6（API）和 M7（UI），交付 B1 功能 F1–F8 全部功能（使用 B1 规范编号；参见 B1↔B5 交叉对照表 `7.0-module-boundaries.en.md § 5`）。

**先决条件**：本阶段开始前必须确定 Q2 技术栈（plan.md § 5 Q2）。默认推荐：FastAPI + HTMX（B5 § 6.2 `[PROPOSED]`）。

**有序实施序列**：

| 步骤 | 模块 | Todo（提案 ID） | 依赖 |
|---|---|---|---|
| 1 | M6 `api` — 核心路由 | `stage2-mrc-api`（现有已阻塞 todo） | P2.2 退出 + Q2 已确定 |
| 2 | M7 `ui` — F1 选择器 + F2 生成 + F3 工作表 | `stage2-mrc-ui-impl`（现有已阻塞 todo） | M6 |
| 3 | M7 `ui` — F4 验证器追踪 + F5 血缘 + F6 中间数据 | `stage2-mrc-ui-impl`（同一 todo，分阶段 PR） | M7 步骤 2 |
| 4 | M7 `ui` — F7 通过/失败 + F8 导出 | `stage2-mrc-ui-impl`（同一 todo，分阶段 PR） | M7 步骤 3 |
| 5 | M7 `ui` — B5.F1 单元格钻取 + B5.F8 基准差异（B5 新增功能） | `stage2-mrc-ui-drill-down-impl` | M7 步骤 4 |

**退出标准**：

- [ ] B1 全部 8 个功能（F1–F8）可访问且对 MRC `2026-04-30` 报告正常运行。
- [ ] B5.F1（单元格钻取）和 B5.F8（基准差异视图）作为 UX 启用功能已实施。
- [ ] 待分析服务商条目以禁用状态显示，并链接至 `docs/<servicer>/_pending.md`（AGENTS § 6.8）。
- [ ] `pytest tests/test_ui_mrc.py -q` 全部通过。
- [ ] 满足 B5 § 4 延迟目标：F1 < 200 ms；F2 < 1 s；F3 < 100 ms；F8 导出 < 5 s。

**涉及模块**：M6, M7。

---

### P2.4 — MRC 验收

**目标**：向用户演示完整 MRC 系统；获得签字；关闭 MRC 里程碑。

**演示检查表**：

- [ ] 使用真实 MRC `2026-04-30` 数据进行 8 个功能（B1 F1–F8）的现场演示。
- [ ] 展示 `BaselineComparison` 差异视图（B5.F8）：与冻结基准比较零差异单元格。
- [ ] 对高亮单元格展示单元格钻取（B5.F1），追踪原始数据 → CTE → 已验证 → 已渲染。
- [ ] 导出 XLSX；验证 `sha256` 与 `baselines/mrc/2026-04-30/validation_report.xlsx` 匹配。
- [ ] `docs/mrc/1.6-baseline.en.md` 中所有 `[VERIFY]` 条目升级为 `[CONFIRMED]`。
- [ ] 用户签字已记录在 `decisions.md` 中。

**退出标准**：

- [ ] 获得用户签字（同步或异步）。
- [ ] `stage2-mrc-acceptance` SQL todo 标记为 `done`。
- [ ] `progress.md` 更新：Phase = P3。
- [ ] `decisions.md` 条目：日期 + "MRC 验收签字"。

**涉及模块**：所有模块（端到端）。

---

### P3 — 多服务商推广

**目标**：使用 P2.0 中预留的可扩展性插槽以及 `8.0-servicer-onboarding.en.md` 中的入驻工作流，入驻 Arvest → CC5 → Selene → SLS → Scattered。

**推广顺序**（暂定；以业务优先级为准）：

| 顺序 | 服务商 | Stage 1 分析 | 快照（G2a 等效） | 预计工作表数 |
|---|---|---|---|---|
| 1 | Arvest | ⏳ 未开始 | ⏳ 未开始 | 4 `[VERIFY]` |
| 2 | CC5 | ⏳ 未开始 | ⏳ 未开始 | 2 `[VERIFY]` |
| 3 | Selene | ⏳ 未开始 | ⏳ 未开始 | 5 `[VERIFY]` |
| 4 | SLS | ⏳ 未开始 | ⏳ 未开始 | 5 `[VERIFY]` |
| 5 | Scattered | ⏳ 未开始 | ⏳ 未开始 | 0（跨服务商） |

每个服务商均遵循 11 步入驻工作流（`8.0-servicer-onboarding.en.md § 3`）。任何 P3 服务商均不应要求修改核心引擎或 UI 代码；任何必要的核心更改均视为针对可扩展性设计的 P0 回归。

---

## 3. 硬性关卡重述

以下来自 `plan.md § 4.2` 的关卡是 Stage 2 实施的硬性先决条件。
**所有** `stage2-mrc-*-impl` todo 均须等待以下关卡**全部**关闭后方可开始：

| 关卡 | 所有者 Todo | 状态 | 阻塞范围 |
|---|---|---|---|
| **G1** — Stage 1 走查评审 | `stage1-mrc-review` | ✅ 已关闭（2026-05-17） | 仅设计；实施仍受 G2/G3 阻塞 |
| **G2a** — 本地输入快照冻结 | `mrc-snapshot` | ⏸ 待操作 | 所有实施工作 |
| **G2b** — 从冻结快照产出基准 XLSX | `mrc-source-baseline` + `mrc-gold` | ⏸ 待操作 | 所有实施工作 |
| **G3** — V1–V12 已验证 | `stage1-mrc-baseline-verify` | ⏸ 等待 G2b | 所有实施工作 |
| **B 系列文档冻结** — B1–B6 全部最终版 | `stage2-mrc-dev-plan`（B6，本文档） | ✅ 进行中 → P2.0 时 done | P2.1 启动 |

> **硬性规则**：G2a + G2b + G3 必须**全部**关闭，才能将任何 `stage2-mrc-*-impl` SQL todo 从 blocked → in_progress（plan.md § 4.2）。

---

## 4. 风险登记册

以下表格纳入了 `plan.md § 3` 中的风险，并新增了 B1–B6 编写期间发现的 Stage 2 专属风险。

| # | 风险 | 严重程度 | Stage 2 缓解措施 |
|---|---|---|---|
| R1 | 无物理基准 XLSX → 单元格一致性验收标准未定义 | 🔴 高 | 由 G2b 关闭；P2.2 单元格一致性套件为验收关卡 |
| R2 | 无本地输入快照 → 引擎无法在无 Redshift 的情况下运行 | 🟠 中 | 由 G2a 关闭；P2.1+ 开发均使用 Parquet shim |
| R2b | 快照完整性——遗漏数据集只在遗留运行时才暴露 | 🟠 中 | G2a 验收标准：禁用 Redshift 运行遗留代码，迭代修补适配器 |
| R3 | 架构意外（如按行追踪需要引擎未发出的额外信息） | 🟠 中 | B1–B6 设计包降低此风险；MB-3 `[VERIFY]` 须在 P2.0 解决 |
| R4 | openpyxl 版本不匹配：≥ 3.1 与 3.0 在 `±inf`/`NaN` 渲染上行为不同 | 🟡 低中 | 由 MB-5 解决及 P2.2 开始时 `pyproject.toml` 锁定关闭 |
| R5 | 构建 Stage 2 期间源仓库 `PrefectFlow` 发生变更 | 🟡 低 | P2.1 前在 `decisions.md` 中冻结 `flow/` git SHA；任何漂移将导致单元格一致性失败 |
| R6 | 可扩展性形状未知，直到第一个非 MRC 服务商验证 | 🟡 低 | B4/B5 注册表和 8 个挂钩点已为多服务商设计；P3 是真实考验 |
| R7 | ch 1.3/1.4/1.5 中 `[VERIFY]` 条目在 P2.2 前未枚举 | 🟡 低 | G3 扫描升级 V1–V12；ch 1.4 中剩余 `[VERIFY]` 在 P2.1/P2.2 期间暴露 |
| R8 | `[FOUND-DURING-B6]` B5 新增两个 prompt-19 F1–F8 未包含的功能（单元格钻取、基准差异视图） | 🟡 低 | 已在 B1↔B5 交叉对照表（`7.0-module-boundaries.en.md § 5`）中记录；两个功能均保留，为 G2b 验收所必需 |
| R9 | Q2 技术栈决策延迟 P2.3 启动 | 🟡 低 | P2.1 和 P2.2 不依赖 Q2；P2.3 设计可以 FastAPI+HTMX 为条件默认先行 |
| R10 | CTE 别名不对称（`p1/p2` 与 `p/p2`，ch 1.2 § 4.1）造成 M3 测试覆盖空白 | 🟡 低 | MB-4 须在 P2.1 中解决；Stage 2 标准化为一致别名方案 |

---

## 5. 决策记录——待确认答案（Q1–Q4）

来自 `plan.md § 5`，附当前状态和阶段影响：

| Q# | 问题 | 当前状态 | 阻塞阶段 |
|---|---|---|---|
| Q1 | **G2 操作**：您是否有 Redshift VPN / Vault 凭证来运行 `freeze_snapshot.py`？还是 G2 需要有生产环境访问权的同事执行？ | ⏸ 未确认——需要操作员行动 | G2a（P2.1 先决条件） |
| Q2 | **UI 技术栈**：Streamlit、FastAPI+HTMX 还是 Next.js+FastAPI？ | ⏸ 未确认——条件默认为 FastAPI+HTMX（B5 § 6.2 `[PROPOSED]`） | P2.3 启动（不阻塞 P2.1/P2.2） |
| Q3 | **openpyxl 版本锁定**：固定为遗留 `flow/` 使用的版本，还是升级到最新 3.1.x + 重新基准？ | ⏸ 未确认——取决于 MB-5（`[VERIFY]` MB-5：黄金 XLSX 使用哪个版本生成？） | P2.2 启动（硬性关卡） |
| Q4 | **G1 走查格式**（如需重新评审）：同步走读还是异步检查表？ | ✅ 实际已解决——G1 于 2026-05-17 暂时关闭；按 AGENTS § 6.11 迭代精化 | 无进一步阻塞 |

**解决流程**：当用户回答 Q1、Q2 或 Q3 时，在 `decisions.md` 中追加条目，并更新对应的 `[VERIFY]` 条目或计划关卡。

---

## 6. 实施 Todo 种子列表（提案——暂不插入）

以下是 Stage 2 实施 SQL `todos` 的完整提案集。**这些仅为设计期提案——P2.0 退出且所有关卡关闭之前不得 `INSERT`。**
依赖图使用 `→` 表示"必须在…之前完成"。

### 6.1 现有已阻塞 todo（7 个 T-C 层级，plan.md § 7.1）

| SQL todo ID | 当前状态 | 阶段 | 说明 |
|---|---|---|---|
| `stage2-mrc-ingestion` | `blocked` | P2.1 步骤 2 | M1：IngestionSource 抽象 + 6 个待分析存根 |
| `stage2-mrc-engine` | `blocked` | P2.1 步骤 4 | M3：5 个 MRC 验证器端到端 |
| `stage2-mrc-xlsx-renderer` | `blocked` | P2.2 步骤 2 | M5：openpyxl 单元格一致性渲染器 |
| `stage2-mrc-cell-identity-harness` | `blocked` | P2.2 步骤 3 | 参数化单元格一致性测试；P2.2 验收关卡 |
| `stage2-mrc-api` | `blocked` | P2.3 步骤 1 | M6：FastAPI 路由枚举 + 服务商状态 API |
| `stage2-mrc-ui-impl` | `blocked` | P2.3 步骤 2–4 | M7：8 功能 UI（F1–F8，按 B1） |
| `stage2-mrc-acceptance` | `blocked` | P2.4 | 端到端验收：基准 + 8 功能演示 + 用户签字 |

### 6.2 P2.0 退出时需新增的 todo

| 提案 SQL todo ID | 阶段 | 说明 | 依赖 |
|---|---|---|---|
| `stage2-mrc-registry-impl` | P2.1 步骤 1 | M8：注册表单例 + 装饰器 + 自动发现 | P2.0 退出关卡 |
| `stage2-mrc-transform-impl` | P2.1 步骤 3 | M2：从 `list[RawTableSnapshot]` 构造 `MrcRemitFrame`；集中时间锚点 | `stage2-mrc-ingestion` |
| `stage2-mrc-cte-harness-impl` | P2.1 步骤 5 | M3 H3：捕获 V2/V3 的 `MrcIntermediateCTEs`；启用 H8 UI 钻取 | `stage2-mrc-engine` |
| `stage2-mrc-sheets-impl` | P2.2 步骤 1 | M4：5 个 `SheetPayload` 组装器；来自 `SheetRegistry` 的列排序；`pandi_diff` 豁免 | `stage2-mrc-engine` |
| `stage2-mrc-ui-drill-down-impl` | P2.3 步骤 5 | M7 B5.F1 单元格钻取 + B5.F8 基准差异（B5 新增，超出 prompt-19 F1–F8） | `stage2-mrc-ui-impl` |

### 6.3 依赖图（提案）

```
P2.0-退出关卡
    │
    ▼
stage2-mrc-registry-impl
    │
    ▼
stage2-mrc-ingestion ──────────────────────────────────────────┐
    │                                                           │
    ▼                                                           │
stage2-mrc-transform-impl                                       │
    │                                                           │
    ▼                                                           │
stage2-mrc-engine ─────────────────────────────────────────────┘
    │                                   │
    ▼                                   ▼
stage2-mrc-cte-harness-impl     stage2-mrc-sheets-impl
                                        │
                                        ▼
                               stage2-mrc-xlsx-renderer
                                        │
                                        ▼
                               stage2-mrc-cell-identity-harness（P2.2 关卡）
                                        │
                                        ▼
                               stage2-mrc-api ──────────────────────────┐
                                        │                                │
                                        ▼                                │
                               stage2-mrc-ui-impl                       │
                                        │                                │
                                        ▼                                │
                               stage2-mrc-ui-drill-down-impl             │
                                        │                                │
                                        └─────────────────────┐          │
                                                              ▼          │
                                                   stage2-mrc-acceptance ◄┘
```

_图 9.0.6.3 — 提案 todo 依赖图。箭头从先决条件流向依赖项。P2.1/P2.2 的两条分支（CTE 套件与工作表）在 `stage2-mrc-engine` 完成后可并行开发。节点 ID 为展示用交叉引用，与上方提案 SQL todo ID 对应。_

**逐步说明：**

1. P2.0 退出关卡必须关闭，注册表实施方可开始。
2. 注册表必须存在，摄入层（M1）才能定位并注册其快照。
3. 摄入与转换（M2）顺序执行；转换依赖摄入输出。
4. 引擎（M3）依赖转换输出；引擎完成后开启两条并行路径。
5. CTE 套件（H3）与工作表组装（M4）可并行进行。
6. XLSX 渲染器（M5）依赖工作表；单元格一致性套件是 P2.2 验收关卡。
7. API（M6）和 UI 实施（M7）在 P2.3 中顺序进行。
8. 钻取 UI 功能（B5.F1 + B5.F8）是验收前的最终 UI 步骤。
9. 验收（P2.4）需要 API/UI 和单元格一致性套件两条路径均完成。

---

## 7. 待确认问题 / `[VERIFY]`

| 编号 | 问题 | 来源 | 影响 | 阶段 |
|---|---|---|---|---|
| DP-1 | `stage2-mrc-registry-impl` 是否应为独立 todo，还是合并入 `stage2-mrc-engine`？（注册表是需要更早就绪的共享基础设施。） | plan.md § 7.1 T-C 层级 | Todo 粒度 | P2.0 |
| DP-2 | P3 推广开始时，每个服务商是否应有独立的 todo 树（如 `stage2-arvest-ingestion`、`stage2-arvest-engine`…）还是合并为单一 `stage2-arvest-onboarding` todo？ | 8.0-servicer-onboarding § 3 | P3 todo 结构 | P2.4 |
| DP-3 | `stage2-mrc-ui-impl` 粒度是否足够，还是应将 F1–F8 拆分为独立 todo 以支持 UI 功能的独立并行开发？ | B2 FR-F{1–8}；B5 § 4 | P2.3 并行性 | P2.3 |
| DP-4 | 如果第一个非 MRC 服务商（Arvest）揭示核心引擎中隐藏的 MRC 假设，P3 是否需要单独的"架构调整"阶段？ | B5 AP1–AP8；R6 | P3 范围 | P3 |
| DP-5 | `stage2-mrc-acceptance` 是否应触发 MRC 最终状态变更（`servicers.yaml` 中 `in-progress → done`）的 AGENTS § 6.8 四产出物提交？ | AGENTS § 6.8；B5 § 3 | P2.4 提交范围 | P2.4 |
