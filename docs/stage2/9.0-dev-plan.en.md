> **Living document** — Follows AGENTS.md § 6.11 3-tier behavior-marker convention.
> `[CONFIRMED]` = verified against source + physical baseline XLSX ·
> `[VERIFY]` = inferred from source only · `[FOUND-DURING-B6]` = discovered during B6 authoring.

> **🔄 2026-05-18 Strategy pivot** — phasing in this doc assumes the long-term ideal
> (frozen-baseline G2a/G2b/G3). Under the current temporary strategy
> (see `10.0-validation-strategy.{zh,en}.md`), G2a/G2b-OLD are deferred and the new
> hard gate is **G2b-LIVE** (comparison harness). Impl-tier phasing P2.1-P2.4 still applies;
> only the validation oracle changes from frozen-baseline to fresh legacy runs.

> **Gate dependency** (AGENTS.md § 6.11 + plan.md § 4.2):
> **P2.1 and all subsequent phases are blocked** until G1 ∧ G2a ∧ G2b ∧ G3 are all closed
> AND all B-docs (B1–B6) are frozen.
> P2.0 (architecture freeze) may proceed as design-only work.
>
> | Gate | Description | Status |
> |---|---|---|
> | G1 | Stage 1 walk-through review | ✅ Closed (provisional sign-off, AGENTS § 6.11) |
> | G2a | Input snapshot freeze (Redshift → local Parquet) | ⏸ Pending operator action |
> | G2b | Physical baseline XLSX freeze | ⏸ Pending operator action |
> | G3 | V1–V12 upgraded to `[CONFIRMED]` | ⏸ Pending user sign-off |

> **Purpose**: Provide the complete phased development plan for Stage 2 (P2.0 → P2.4 → P3),
> refined with module boundaries from 7.0-module-boundaries, gate prerequisites, exit criteria,
> risk register, pending decision log, and implementation todo seed proposals.
>
> **Intended audience**: All Stage 2 implementers; tech leads; the user reviewing B6 deliverables;
> future-session agents resuming Stage 2 work.
>
> **Revision history**
>
> | Date | Author | Change |
> |---|---|---|
> | 2026-05-28 | Copilot CLI agent | v1 — Initial version (B6). Restates plan.md § 4.4 phasing with module-level detail from 7.0-module-boundaries. Adds exit criteria, risk register refined from plan.md § 3, decision log Q1–Q4, and T-C todo seed proposals. |

# 9.0 Development Plan (Stage 2)

---

## 1. Phasing Overview

The phasing below restates `plan.md § 4.4` (session `4cd52a8e-d034-4def-84a0-04057dd64872`)
with module-level granularity from `7.0-module-boundaries.en.md`.

```
G1 ∧ G2a ∧ G2b ∧ G3 all closed
∧ B-docs B1–B6 all frozen (P2.0 exit criterion)
              │
              ▼
P2.0  Architecture Freeze
      data-model + engine API + UI contract sign-off
      (design only — no implementation code)
              │
              ▼
P2.1  MRC Engine Implementation
      M1 ingestion + M2 transform + M3 validators
      + M8 registry → 5 sheets → DataFrame payload
              │
              ▼
P2.2  XLSX Renderer Implementation
      M4 sheets + M5 renderer → cell-identity acceptance
      (openpyxl version pinned at this phase)
              │
              ▼
P2.3  UI Implementation (8 features)
      M6 API + M7 UI (tech-stack decision required before start)
              │
              ▼
P2.4  MRC Acceptance
      G2 baseline + 8-feature demo + user sign-off
              │
              ▼
P3    Multi-servicer Rollout
      Arvest → CC5 → Selene → SLS → Scattered
      (uses extensibility slots reserved in P2.0)
```

_Figure 9.0.1 — Stage 2 phasing diagram. Each arrow requires the phase above to reach its exit criteria. G-gates are explicit hard prerequisites, not soft dependencies._

---

## 2. Per-Phase Deliverables and Exit Criteria

### P2.0 — Architecture Freeze

**Goal**: Confirm that B1–B6 design artefacts are internally consistent, cross-references are resolved,
all `[VERIFY]` items that can be resolved before G2 are resolved, and the module boundary contract
(B6 `7.0-module-boundaries.en.md`) is signed off.

**Deliverables** (the following B-docs constitute the architecture freeze):

| Document | Status at P2.0 exit |
|---|---|
| `docs/stage2/1.0-feature-list.{zh,en}.md` | Final; B1↔B5 crosswalk understood |
| `docs/stage2/2.0-srs.{zh,en}.md` | Final; all 37 FR + 12 NFR stable |
| `docs/stage2/3.0-data-model.{zh,en}.md` | Final; B3 model schemas stable |
| `docs/stage2/4.0-validator-registry.{zh,en}.md` | Final; registry shapes stable |
| `docs/stage2/5.0-extensibility-spec.{zh,en}.md` | Final; 8 hook points stable |
| `docs/stage2/6.0-ui-architecture.{zh,en}.md` | Final except Q2 tech-stack choice |
| `docs/stage2/7.0-module-boundaries.{zh,en}.md` | **Final; this is the P2.0 contract** |
| `docs/stage2/8.0-servicer-onboarding.{zh,en}.md` | Final for onboarding workflow |
| `docs/stage2/9.0-dev-plan.{zh,en}.md` | Final; this document |

**Exit criteria**:

- [ ] G1 ∧ G2a ∧ G2b ∧ G3 all closed.
- [ ] All B-docs above pass `tools/stage_doc_checks.py` with zero P0 failures.
- [ ] `pytest -q` all green (no Stage 2 code yet — only Stage 1 regression tests).
- [ ] `mkdocs build --strict` succeeds with all 9 document pairs in `docs/stage2/`.
- [ ] Open questions MB-2 and MB-3 (module boundary `7.0-module-boundaries.en.md § 7`) resolved.
- [ ] Q2 tech-stack decision resolved (plan.md § 5 Q2) — required to unlock P2.3 design but not P2.1/P2.2.
- [ ] `decisions.md` entry added for every resolution above.

**Modules involved**: design only; no code.

---

### P2.1 — MRC Engine Implementation

**Goal**: Implement M1 (ingestion), M2 (transform), M3 (validators), M8 (registry) end-to-end,
producing a `list[ValidatorResult]` from a frozen Parquet snapshot.

**Ordered implementation sequence**:

| Step | Module | Todo (proposed ID) | Dependency |
|---|---|---|---|
| 1 | M8 `registry` | `stage2-mrc-registry-impl` | P2.0 exit |
| 2 | M1 `ingestion` | `stage2-mrc-ingestion` (existing blocked todo) | M8 |
| 3 | M2 `transform` | `stage2-mrc-transform-impl` | M1 |
| 4 | M3 `validators` (5 validators) | `stage2-mrc-engine` (existing blocked todo) | M2, M8 |
| 5 | M3 `validators` CTE harness (H3) | `stage2-mrc-cte-harness-impl` | M3 step 4 |

**Cell-identity harness gates each step**: after each module, `pytest tests/test_cell_identity.py`
is run against mock fixtures; a full real-baseline run follows only after G2b closes.

**Exit criteria**:

- [ ] All 5 MRC validators execute without error against `tests/fixtures/` synthetic data.
- [ ] `ValidatorResult.cell_annotations` matches hand-verified expected highlight set for the
  synthetic fixture.
- [ ] `MrcIntermediateCTEs` (B3 § 2.5) is captured for V2 and V3 (H3 hook); V1/V5 return `None`.
- [ ] `pytest tests/test_engine_mrc.py -q` all green.
- [ ] `ruff check .` zero violations.
- [ ] `mypy --strict whitebox/` (new code only) zero errors.
- [ ] No `if/elif` branching on `ServicerId` string inside any M1–M3 code (anti-pattern AP1).

**Modules**: M1, M2, M3, M8.

---

### P2.2 — XLSX Renderer Implementation

**Goal**: Implement M4 (sheets) and M5 (renderer) such that the XLSX output is cell-identical to
`baselines/mrc/2026-04-30/validation_report.xlsx` across all 5 sheets.

**Critical action at P2.2 start**: **pin `openpyxl` version** in `pyproject.toml` to match the
version used to produce the gold baseline XLSX (resolved by MB-5 in P2.0 exit).  See plan.md § 3
risk R4.

**Ordered implementation sequence**:

| Step | Module | Todo (proposed ID) | Dependency |
|---|---|---|---|
| 1 | M4 `sheets` (5 payload assemblers) | `stage2-mrc-sheets-impl` | P2.1 exit |
| 2 | M5 `renderer` (openpyxl) | `stage2-mrc-xlsx-renderer` (existing blocked todo) | M4 |
| 3 | Cell-identity harness (acceptance gate) | `stage2-mrc-cell-identity-harness` (existing blocked todo) | M5 |

**Exit criteria**:

- [ ] `openpyxl` version pinned in `pyproject.toml`; `uv.lock` updated; version recorded in `decisions.md`.
- [ ] `BaselineComparison.passed == True` for all 5 sheets against `baselines/mrc/2026-04-30/validation_report.xlsx`.
- [ ] All V1–V12 rendering attributes (ch 1.6 § 9) upgraded from `[VERIFY]` to `[CONFIRMED]`.
- [ ] `pytest tests/test_cell_identity.py -q` all green (P0 acceptance gate).
- [ ] `pandi_diff` highlight exemption (ch 1.5 § 3.3) passes cell-identity check.

**Modules**: M4, M5.

---

### P2.3 — UI Implementation (8 Features)

**Goal**: Implement M6 (API) and M7 (UI) delivering all B1 features F1–F8 (using B1 canonical
numbering; see B1↔B5 crosswalk in `7.0-module-boundaries.en.md § 5`).

**Precondition**: Q2 tech-stack decision must be answered before this phase starts (plan.md § 5 Q2).
Default recommendation: FastAPI + HTMX (B5 § 6.2 `[PROPOSED]`).

**Ordered implementation sequence**:

| Step | Module | Todo (proposed ID) | Dependency |
|---|---|---|---|
| 1 | M6 `api` — core routes | `stage2-mrc-api` (existing blocked todo) | P2.2 exit + Q2 answered |
| 2 | M7 `ui` — F1 picker + F2 generate + F3 sheets | `stage2-mrc-ui-impl` (existing blocked todo) | M6 |
| 3 | M7 `ui` — F4 validator trace + F5 lineage + F6 intermediate | `stage2-mrc-ui-impl` (same todo, phased PRs) | M7 step 2 |
| 4 | M7 `ui` — F7 pass/fail + F8 export | `stage2-mrc-ui-impl` (same todo, phased PRs) | M7 step 3 |
| 5 | M7 `ui` — B5.F1 cell drill-down + B5.F8 baseline diff (new B5 features) | `stage2-mrc-ui-drill-down-impl` | M7 step 4 |

**Exit criteria**:

- [ ] All 8 B1 features (F1–F8) accessible and functional against the MRC report for `2026-04-30`.
- [ ] B5.F1 (cell drill-down) and B5.F8 (baseline diff) implemented as UX enabling features.
- [ ] Pending-servicer entries appear disabled with link to `docs/<servicer>/_pending.md` (AGENTS § 6.8).
- [ ] `pytest tests/test_ui_mrc.py -q` all green.
- [ ] Latency targets from B5 § 4 met: F1 < 200 ms; F2 < 1 s; F3 < 100 ms; F8 export < 5 s.

**Modules**: M6, M7.

---

### P2.4 — MRC Acceptance

**Goal**: Demonstrate the complete MRC system to the user; obtain sign-off; close the MRC milestone.

**Demo checklist**:

- [ ] Live demo of all 8 features (B1 F1–F8) using real MRC `2026-04-30` data.
- [ ] Show `BaselineComparison` diff view (B5.F8): zero delta cells against frozen baseline.
- [ ] Show cell drill-down (B5.F1) for a highlighted cell, tracing raw → CTE → validated → rendered.
- [ ] Export XLSX; verify `sha256` matches `baselines/mrc/2026-04-30/validation_report.xlsx`.
- [ ] All `[VERIFY]` items in `docs/mrc/1.6-baseline.en.md` upgraded to `[CONFIRMED]`.
- [ ] User sign-off recorded in `decisions.md`.

**Exit criteria**:

- [ ] User sign-off obtained (synchronous or async).
- [ ] `stage2-mrc-acceptance` SQL todo marked `done`.
- [ ] `progress.md` updated: Phase = P3.
- [ ] `decisions.md` entry: date + "MRC acceptance sign-off".

**Modules**: all (end-to-end).

---

### P3 — Multi-Servicer Rollout

**Goal**: Onboard Arvest → CC5 → Selene → SLS → Scattered using the extensibility slots reserved
in P2.0 and the onboarding workflow from `8.0-servicer-onboarding.en.md`.

**Rollout order** (provisional; subject to business priority):

| Order | Servicer | Stage 1 analysis | Snapshot (G2a-equiv) | Estimated sheets |
|---|---|---|---|---|
| 1 | Arvest | ⏳ not started | ⏳ not started | 4 `[VERIFY]` |
| 2 | CC5 | ⏳ not started | ⏳ not started | 2 `[VERIFY]` |
| 3 | Selene | ⏳ not started | ⏳ not started | 5 `[VERIFY]` |
| 4 | SLS | ⏳ not started | ⏳ not started | 5 `[VERIFY]` |
| 5 | Scattered | ⏳ not started | ⏳ not started | 0 (cross-servicer) |

Each servicer goes through the 11-step onboarding workflow (`8.0-servicer-onboarding.en.md § 3`).
No core engine or UI code should require modification for any P3 servicer; any required core change
is a P0 regression against the extensibility design.

---

## 3. Hard Gates Restated

The following gates from `plan.md § 4.2` are binding prerequisites for Stage 2 implementation.
No `stage2-mrc-*-impl` todo may begin until **all** of the following are closed:

| Gate | Owner todo(s) | Status | Blocks |
|---|---|---|---|
| **G1** — Stage 1 walk-through review | `stage1-mrc-review` | ✅ Closed (2026-05-17) | Design only; implementation still blocked on G2/G3 |
| **G2a** — Local input snapshot frozen | `mrc-snapshot` | ⏸ Pending | All implementation |
| **G2b** — Output baseline XLSX produced from frozen snapshot | `mrc-source-baseline` + `mrc-gold` | ⏸ Pending | All implementation |
| **G3** — V1–V12 verified | `stage1-mrc-baseline-verify` | ⏸ Pending G2b | All implementation |
| **B-docs frozen** — B1–B6 all final | `stage2-mrc-dev-plan` (B6, this doc) | ✅ In progress → done at P2.0 | P2.1 start |

> **Hard rule**: G2a + G2b + G3 must be **all** closed before opening any `stage2-mrc-*-impl` SQL
> todo from blocked → in_progress (plan.md § 4.2).

---

## 4. Risk Register

The table below incorporates risks from `plan.md § 3` and adds Stage-2-specific risks discovered
during B1–B6 authoring.

| # | Risk | Severity | Stage-2 mitigation |
|---|---|---|---|
| R1 | No physical baseline XLSX → cell-identity acceptance undefined | 🔴 HIGH | Closed by G2b; P2.2 cell-identity harness is the acceptance gate |
| R2 | No local input snapshot → cannot run engine without live Redshift | 🟠 MED | Closed by G2a; all P2.1+ dev uses Parquet shim |
| R2b | Snapshot exhaustiveness — missed dataset only surfaces at legacy run | 🟠 MED | G2a acceptance criterion: run legacy code with Redshift disabled, patch adapter iteratively |
| R3 | Architectural surprises (e.g. per-row trace requires more than engine emits) | 🟠 MED | B1–B6 design package reduces this; MB-3 `[VERIFY]` must be resolved in P2.0 |
| R4 | `openpyxl` version mismatch: ≥ 3.1 vs 3.0 `±inf`/`NaN` rendering | 🟡 LOW-MED | Closed by MB-5 resolution and `pyproject.toml` pin at P2.2 start |
| R5 | Source repo `PrefectFlow` drift while building Stage 2 | 🟡 LOW | Freeze `flow/` git SHA in `decisions.md` before P2.1; any drift surfaces as cell-identity failure |
| R6 | Extensibility shape unknown until first non-MRC servicer | 🟡 LOW | B4/B5 registries and 8 hook points designed for multi-servicer; real test in P3 |
| R7 | `[VERIFY]` items inside ch 1.3/1.4/1.5 not yet enumerated for P2.2 | 🟡 LOW | G3 sweep upgrades V1–V12; remaining `[VERIFY]` in ch 1.4 surface during P2.1/P2.2 |
| R8 | `[FOUND-DURING-B6]` B5 added two features (cell drill-down, baseline diff) not in prompt-19 F1–F8 | 🟡 LOW | Documented in B1↔B5 crosswalk (`7.0-module-boundaries.en.md § 5`); both features are retained as required for G2b acceptance |
| R9 | Q2 tech-stack decision delays P2.3 start | 🟡 LOW | P2.1 and P2.2 do not depend on Q2; P2.3 design can begin with FastAPI+HTMX as conditional default |
| R10 | CTE-naming asymmetry (`p1/p2` vs `p/p2`, ch 1.2 § 4.1) creates M3 test coverage gap | 🟡 LOW | MB-4 must be resolved in P2.1; normalise to consistent alias scheme in Stage 2 |

---

## 5. Decision Log — Pending Answers (Q1–Q4)

Restated from `plan.md § 5` with current status and phase impact:

| Q# | Question | Current state | Blocks phase |
|---|---|---|---|
| Q1 | **G2 logistics**: Do you have Redshift VPN / Vault credentials to run `freeze_snapshot.py`? Or does G2 require a colleague with prod access? | ⏸ Unanswered — operator action required | G2a (precondition for P2.1) |
| Q2 | **UI tech-stack**: Streamlit vs FastAPI+HTMX vs Next.js+FastAPI? | ⏸ Unanswered — conditional default is FastAPI+HTMX (B5 § 6.2 `[PROPOSED]`) | P2.3 start (does not block P2.1/P2.2) |
| Q3 | **`openpyxl` version pin**: Fix to legacy `flow/` version or jump to latest 3.1.x + re-baseline? | ⏸ Unanswered — depends on MB-5 (`[VERIFY]` MB-5: what version produced the gold XLSX?) | P2.2 start (hard gate) |
| Q4 | **Walk-through format for G1** (if re-review needed): synchronous vs async checklist? | ✅ Effectively resolved — G1 closed provisionally 2026-05-17; refinement per AGENTS § 6.11 | No further block |

**Resolution process**: when the user answers Q1, Q2, or Q3, append a `decisions.md` entry
and update the corresponding `[VERIFY]` item or plan gate.

---

## 6. Implementation Todo Seed List (Proposed — Do Not Insert Yet)

The following is the proposed complete set of SQL `todos` for Stage 2 implementation.
**These are design-time proposals only — do not `INSERT` until P2.0 exit and all gates close.**
Dependency graph uses `→` to mean "must complete before".

### 6.1 Existing blocked todos (7 T-C tier, plan.md § 7.1)

| SQL todo id | Current status | Phase | Description |
|---|---|---|---|
| `stage2-mrc-ingestion` | `blocked` | P2.1 step 2 | M1: IngestionSource abstraction + 6 pending stubs |
| `stage2-mrc-engine` | `blocked` | P2.1 step 4 | M3: 5 MRC validators end-to-end |
| `stage2-mrc-xlsx-renderer` | `blocked` | P2.2 step 2 | M5: openpyxl cell-identical renderer |
| `stage2-mrc-cell-identity-harness` | `blocked` | P2.2 step 3 | Parameterised cell-identity test; P2.2 acceptance gate |
| `stage2-mrc-api` | `blocked` | P2.3 step 1 | M6: FastAPI routes enumeration + servicer status API |
| `stage2-mrc-ui-impl` | `blocked` | P2.3 steps 2–4 | M7: 8-feature UI (F1–F8 per B1) |
| `stage2-mrc-acceptance` | `blocked` | P2.4 | End-to-end acceptance: baseline + 8-feature demo + user sign-off |

### 6.2 New todos to insert at P2.0 exit

| Proposed SQL todo id | Phase | Description | Depends on |
|---|---|---|---|
| `stage2-mrc-registry-impl` | P2.1 step 1 | M8: registry singletons + `@register_validator` / `@register_sheet` / `@register_field_mapping` / `@register_rule` decorators + auto-discovery | P2.0 exit gates |
| `stage2-mrc-transform-impl` | P2.1 step 3 | M2: `MrcRemitFrame` constructor from `list[RawTableSnapshot]`; time-anchor centralisation | `stage2-mrc-ingestion` |
| `stage2-mrc-cte-harness-impl` | P2.1 step 5 | M3 H3: capture `MrcIntermediateCTEs` for V2/V3; H8 UI drill-down enablement | `stage2-mrc-engine` |
| `stage2-mrc-sheets-impl` | P2.2 step 1 | M4: 5 `SheetPayload` assemblers; column ordering from `SheetRegistry`; `pandi_diff` exemption | `stage2-mrc-engine` |
| `stage2-mrc-ui-drill-down-impl` | P2.3 step 5 | M7 B5.F1 cell drill-down + B5.F8 baseline diff (B5 additions beyond prompt-19 F1–F8) | `stage2-mrc-ui-impl` |

### 6.3 Dependency graph (proposed)

```
P2.0-exit-gates
    │
    ▼
stage2-mrc-registry-impl
    │
    ▼
stage2-mrc-ingestion ──────────────────────────────────────────┐
    │                                                           │
    ▼                                                           │
stage2-mrc-transform-impl                                       │
    │                                                           │
    ▼                                                           │
stage2-mrc-engine ─────────────────────────────────────────────┘
    │                                   │
    ▼                                   ▼
stage2-mrc-cte-harness-impl     stage2-mrc-sheets-impl
                                        │
                                        ▼
                               stage2-mrc-xlsx-renderer
                                        │
                                        ▼
                               stage2-mrc-cell-identity-harness (P2.2 gate)
                                        │
                                        ▼
                               stage2-mrc-api ──────────────────────────┐
                                        │                                │
                                        ▼                                │
                               stage2-mrc-ui-impl                       │
                                        │                                │
                                        ▼                                │
                               stage2-mrc-ui-drill-down-impl             │
                                        │                                │
                                        └─────────────────────┐          │
                                                              ▼          │
                                                   stage2-mrc-acceptance ◄┘
```

_Figure 9.0.6.3 — Proposed todo dependency graph. Arrows flow from prerequisite to dependent. The two P2.1/P2.2 branches (CTE harness and sheets) are independent and may be developed in parallel after `stage2-mrc-engine` completes. Node IDs are display-only cross-references; they match the proposed SQL todo IDs above._

**Step-by-step explanation:**

1. P2.0 exit gates must close before registry implementation begins.
2. Registry must exist before ingestion (M1) can locate and register its snapshots.
3. Ingestion and transform (M2) execute sequentially; transform depends on ingestion output.
4. Engine (M3) depends on transform output; once engine is done, two parallel paths open.
5. CTE harness (H3) and sheets assembly (M4) proceed in parallel.
6. XLSX renderer (M5) depends on sheets; cell-identity harness is the P2.2 acceptance gate.
7. API (M6) and UI impl (M7) proceed sequentially in P2.3.
8. Drill-down UI features (B5.F1 + B5.F8) are the final UI step before acceptance.
9. Acceptance (P2.4) requires both API/UI and cell-identity harness paths complete.

---

## 7. Open Questions / `[VERIFY]`

| ID | Question | Source | Impact | Phase |
|---|---|---|---|---|
| DP-1 | Should `stage2-mrc-registry-impl` be a separate todo or folded into `stage2-mrc-engine`? (Registry is shared infrastructure needed earlier.) | plan.md § 7.1 T-C tier | Todo granularity | P2.0 |
| DP-2 | When P3 rollout begins, should each servicer get its own todo tree (e.g. `stage2-arvest-ingestion`, `stage2-arvest-engine`, …) or share a combined `stage2-arvest-onboarding` todo? | 8.0-servicer-onboarding § 3 | P3 todo structure | P2.4 |
| DP-3 | Is `stage2-mrc-ui-impl` granular enough, or should F1–F8 be separate todos to allow independent parallel work on UI features? | B2 FR-F{1–8}; B5 § 4 | P2.3 parallelism | P2.3 |
| DP-4 | Does P3 require a separate "architecture adjustment" phase if the first non-MRC servicer (Arvest) reveals hidden MRC assumptions in the core engine? | B5 AP1–AP8; R6 | P3 scope | P3 |
| DP-5 | Should `stage2-mrc-acceptance` trigger the AGENTS § 6.8 four-artifact commit for MRC's final status change (`in-progress → done` in `servicers.yaml`)? | AGENTS § 6.8; B5 § 3 | P2.4 commit scope | P2.4 |
